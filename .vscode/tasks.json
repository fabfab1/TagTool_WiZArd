{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "build",
      "command": "gcc",
      "args": [
        "-g",
        "${workspaceFolder}/main.cpp",
        "-o",
        "${workspaceFolder}/.compiler/main",
        "-lstdc++"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
